# OpenBox 开源工具箱 — 产品需求文档

> **版本:** v0.5.1  
> **状态:** 草稿  
> **日期:** 2026-07-07  

---

## 1. 文档概述

### 1.1 目的

本文档定义 OpenBox 可扩展桌面工具箱的产品需求，明确产品定位、功能范围、架构设计与非功能约束，为开发迭代和社区协作提供统一参考。

### 1.2 范围

本 PRD 覆盖 OpenBox v0.5.1 的核心功能，包括插件系统、桌面 UI、CLI 工具、数据持久化、安全权限模型。不涉及移动端、Web 端或云端服务。

### 1.3 术语表

| 术语 | 说明 |
|------|------|
| 插件 (Plugin) | 遵循 OpenBox 约定的可扩展模块，包含主进程后端 + 渲染进程 UI |
| 沙箱 (Sandbox) | 插件的执行隔离环境，支持进程内和子进程两种模式 |
| 清单 (Manifest) | `plugin.json`，定义插件的元信息、入口、权限和配置模式 |
| 主进程 (Main Process) | Electron 主进程，负责系统操作、数据库、插件管理 |
| 渲染进程 (Renderer) | Electron 渲染进程，负责 UI 展示 |

---

## 2. 产品定位

### 2.1 背景

开发者桌面工具日益碎片化，每个工具独立安装、互不关联。OpenBox 旨在提供一个 **统一的插件化平台**，让开发者通过安装插件即可快速扩展功能，无需在多个独立应用之间切换。

### 2.2 目标用户

| 用户类型 | 特征 | 核心诉求 |
|----------|------|----------|
| 普通用户 | 使用现成插件的终用户 | 一键安装、直观管理、按需启禁 |
| 插件开发者 | 编写插件的开发者 | 快速脚手架、即时调试、清晰 API |

### 2.3 核心价值

- **可扩展** — 一切功能皆插件，按需安装，自由组合
- **轻量** — 基于 Electron + React，启动快、资源占用低
- **安全** — 权限声明与运行时检查，插件行为可控
- **开发者友好** — CLI 脚手架 + 开发模式，降低插件开发门槛

---

## 3. 功能需求

### 3.1 插件管理

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| FM-01 | 插件安装 | P0 | 支持通过 `.zip` 文件（拖拽/选择文件）和目录选择两种方式安装 |
| FM-02 | 插件卸载 | P0 | 删除插件目录及数据库记录，移除前反激活 |
| FM-03 | 插件启用/禁用 | P0 | 切换插件的激活状态，禁用时不加载主进程和 UI |
| FM-04 | 插件列表 | P0 | 展示所有已安装插件，包含名称、版本、描述、作者、状态 |
| FM-05 | 插件配置 | P0 | 根据清单中的 `config` schema 自动生成配置表单 |
| FM-06 | 插件详情 | P1 | 查看单个插件的完整信息和操作入口 |
| FM-07 | 插件搜索 | P2 | 按名称/描述搜索已安装插件 |

### 3.2 插件运行时

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| RT-01 | 生命周期管理 | P0 | 安装 → 激活 → 运行 → 反激活 → 卸载 |
| RT-02 | 进程内执行 | P0 | 默认在 Electron 主进程中 `import()` 加载插件，传递完整上下文 |
| RT-03 | 子进程执行 | P1 | 通过 `child_process.fork()` 隔离插件，IPC 通信，30s 超时 |
| RT-04 | 消息传递 | P0 | 渲染进程 ↔ 主进程 ↔ 插件之间双向消息通信 |
| RT-05 | 自定义协议 | P0 | `plugin://` 协议托管插件静态文件，渲染进程动态加载插件 UI |
| RT-06 | 事件总线 | P1 | 主进程内轻量级 pub/sub，支持插件间通信 |
| RT-07 | 全局快捷键 | P2 | 插件可注册全局键盘快捷键 |

### 3.3 桌面 UI

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| UI-01 | 首页 (Home) | P0 | 已启用插件的卡片网格，支持空状态引导 |
| UI-02 | 管理页 (Market) | P0 | 导入插件、查看所有插件、启禁配置删除 |
| UI-03 | 插件详情页 | P0 | 展示插件完整信息并挂载插件 UI 组件 |
| UI-04 | 设置页 | P1 | 显示应用基本信息（版本、平台、插件目录） |
| UI-05 | 侧边栏导航 | P0 | 可折叠侧边栏，含已安装/管理/设置入口 |
| UI-06 | 配置弹窗 | P0 | 自动根据 schema 生成 Ant Design 表单 |
| UI-07 | 导入弹窗 | P0 | 拖拽区域 + 文件选择 + 目录选择 |
| UI-08 | 插件 UI 容器 | P0 | PluginHost 组件，动态抓取并渲染插件 UI |

### 3.4 CLI 开发者工具

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| CLI-01 | 创建插件脚手架 | P0 | `openbox create-plugin <name>` 从模板生成插件项目 |
| CLI-02 | 开发模式 | P0 | `openbox dev [path]` 监听 src 变更自动重编译 |
| CLI-03 | 模板完整性 | P0 | 包含 plugin.json、tsconfig、src/main.ts、src/renderer.tsx、类型声明 |

### 3.5 数据持久化

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| DB-01 | 插件元数据 | P0 | 存储插件的完整信息、启用状态、安装时间等 |
| DB-02 | 设置存储 | P0 | 键值对存储应用级别设置 |
| DB-03 | 日志记录 | P1 | 按插件记录运行日志，含级别和时间戳 |
| DB-04 | 数据库迁移 | P0 | 启动时自动执行增量迁移（幂等） |
| DB-05 | 持久化写盘 | P0 | 每次写操作后同步写回磁盘文件 |

### 3.6 安全与权限

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| SC-01 | 权限声明 | P0 | 插件清单必须声明所需权限 |
| SC-02 | 运行时检查 | P0 | 每次敏感操作前检查权限，未声明则拒绝 |
| SC-03 | 上下文隔离 | P0 | Electron contextIsolation 开启，禁用 nodeIntegration |
| SC-04 | 内容安全策略 | P0 | CSP 仅允许 `self` 和 `plugin:` 协议加载脚本 |
| SC-05 | 权限粒度 | P0 | database:read/write、shell:exec、network:fetch、notification、clipboard、dialog、shortcut |

---

## 4. 非功能需求

| ID | 需求 | 指标 |
|----|------|------|
| NF-01 | 启动时间 | 冷启动 < 3s（不含插件加载） |
| NF-02 | 插件加载 | 单插件激活 < 500ms |
| NF-03 | 内存占用 | 空闲状态 < 100MB |
| NF-04 | 磁盘占用 | 应用本体 < 150MB |
| NF-05 | 安全性 | SQL 注入防护、路径穿越防护、CSP 策略 |
| NF-06 | 兼容性 | Windows 10/11 主流版本 |
| NF-07 | 可扩展性 | 插件系统 API 向前兼容，版本化声明 |

---

## 5. 系统架构

### 5.1 整体架构

```
┌─────────────────────────────────────────────────┐
│               渲染进程 (Renderer)                  │
│  React 19 + Ant Design + Zustand                 │
│  ┌──────────┐ ┌──────────┐ ┌────────────────┐   │
│  │ Home     │ │ Market   │ │ PluginView     │   │
│  │ 页面     │ │ 管理页   │ │ 详情页         │   │
│  └──────────┘ └──────────┘ └────────────────┘   │
│        │             │              │             │
│        └─────────────┼──────────────┘             │
│                      │ IPC                        │
│              window.electronAPI                   │
└──────────────────────┼───────────────────────────┘
                       │
┌──────────────────────┼───────────────────────────┐
│               主进程 (Main Process)                 │
│  ┌─────────────────────────────────────────┐      │
│  │         PluginManager                    │      │
│  │  安装 / 激活 / 反激活 / 消息转发         │      │
│  └────────────┬────────────────────────────┘      │
│               │                                    │
│  ┌────────────┴────────────┐  ┌────────────────┐  │
│  │   PluginSandbox         │  │  EventBus      │  │
│  │  进程内 / 子进程隔离     │  │  事件总线      │  │
│  └────────────┬────────────┘  └────────────────┘  │
│               │                                    │
│  ┌────────────┴────────────┐  ┌────────────────┐  │
│  │   sql.js (SQLite)       │  │  Permission    │  │
│  │   数据库层              │  │  权限守卫      │  │
│  └─────────────────────────┘  └────────────────┘  │
└────────────────────────────────────────────────────┘

┌────────────────────────────────────────────┐
│            CLI 工具 (命令行)                 │
│  create-plugin  /  dev                     │
└────────────────────────────────────────────┘
```

### 5.2 核心模块职责

| 模块 | 职责 |
|------|------|
| PluginManager | 插件生命周期管理、目录扫描、清单校验、沙箱创建 |
| PluginSandbox | 插件执行环境封装，屏蔽底层隔离细节 |
| EventBus | 主进程内的发布订阅通信 |
| PermissionGuard | 权限校验拦截器 |
| PluginProtocol | 自定义 `plugin://` 协议，服务插件静态文件 |
| Database | sql.js 封装，提供 CRUD 和迁移 |
| IPC Layer | invoke/handle + push 模式的进程间通信 |
| PluginHost (Renderer) | 动态获取并渲染插件的 React 组件 |

### 5.3 数据流

```
安装流程:
  用户选择 .zip → Renderer → IPC → Main (PluginManager)
  → 解压到插件目录 → 校验 manifest → 写入 DB → 返回结果

激活流程:
  应用启动 / 用户启用 → PluginManager → 创建 Sandbox
  → import() 插件主模块 → 调用 activate(ctx) → 标记启用

UI 加载流程:
  用户打开插件 → PluginView → PluginHost
  → fetch(plugin://xxx/renderer.js) → 执行模块 → 渲染组件

消息通信:
  Renderer → IPC → PluginManager → PluginSandbox → 插件
  插件 → EventBus → IPC push → Renderer
```

---

## 6. 用户交互流程

### 6.1 普通用户流程

```
首次使用:
  启动 → 首页空状态 → 导航至管理页 → 点击导入
  → 选择 .zip 文件 → 安装成功 → 插件出现在首页

日常使用:
  启动 → 首页查看已启用插件 → 点击插件卡片进入详情
  → 使用插件功能 / 配置 / 禁用 / 卸载
```

### 6.2 插件开发者流程

```
创建:
  openbox create-plugin my-tool
  → 生成模板项目 → 编辑 src/main.ts 和 src/renderer.tsx

开发:
  cd my-tool → openbox dev → 修改代码自动重编译
  → 在 OpenBox 中安装 → 实时查看效果

发布:
  编译 → 打包为 .zip → 分享给其他用户导入
```

---

## 7. 约束与假设

### 7.1 技术约束

- Electron 34 + React 19 的技术栈固定，避免重大版本跳升
- sql.js 的同步 API 限制了子进程模式的默认关闭
- Windows 为主平台，macOS/Linux 兼容性需后续验证

### 7.2 设计假设

- 插件数量通常 < 50 个，单插件 UI 复杂度有限
- 插件开发者有一定的 TypeScript 和 React 基础
- 用户场景以开发者工具为主，不涉及媒体处理或游戏

### 7.3 边界条件

| 场景 | 处理方式 |
|------|----------|
| 插件安装失败（manifest 无效） | 返回明确错误，清理残留文件 |
| 插件激活崩溃 | 捕获异常，标记错误状态，不影响其他插件 |
| 数据库损坏 | 启动时检测，自动备份并重建 |
| 同名插件冲突 | 安装时检测，提示覆盖或取消 |

---

## 8. 未来迭代方向

| 方向 | 说明 | 优先级 |
|------|------|--------|
| 插件市场 | 在线商店，支持浏览/搜索/一键安装 | P2 |
| 插件更新机制 | 检查更新、增量下载、热更新 | P2 |
| 多语言支持 | i18n 框架，插件也可本地化 | P2 |
| macOS/Linux 支持 | 跨平台测试与适配 | P2 |
| 主题系统 | 浅色/深色/自定义主题 | P3 |
| 性能面板 | 插件资源占用监控 | P3 |
| 插件分组 | 按标签/用途分组管理 | P3 |
| 云同步 | 插件配置跨设备同步 | P4 |

---

## 9. 附录

### 9.1 参考文档

- [Electron 官方文档](https://www.electronjs.org/docs)
- [React 19 Release Notes](https://react.dev/blog/2024/12/05/react-19)
- [Ant Design 5.x](https://ant.design/docs/react/introduce)
- [sql.js 文档](https://sql.js.org/documentation/)

### 9.2 修订历史

| 版本 | 日期 | 说明 | 作者 |
|------|------|------|------|
| v0.1 | 2026-07-07 | 初稿 | opencode |
