# OpenBox 使用说明

> **版本**: v1.0.1 | **平台**: Windows x64 | **许可证**: MIT

---

## 1. 软件简介

OpenBox 是一款**基于插件架构的轻量级桌面工具箱**，采用 Electron + React + TypeScript 技术栈。它允许用户通过插件系统动态安装、管理和运行各类功能插件，将多个小工具整合到一个统一的桌面应用中。

### 功能特性

| 特性 | 说明 |
|------|------|
| **插件化架构** | 宿主与插件解耦，插件按需安装/启用/停用/卸载，无需修改宿主代码 |
| **双进程模型** | 每个插件拥有独立的主进程逻辑和渲染进程 UI，通过 IPC 通信 |
| **安全沙箱** | 渲染进程通过 `contextBridge` 限制 API，插件使用 `PermissionGuard` 做权限校验 |
| **可视化插件管理** | 图形化界面管理插件，支持拖拽导入 .zip 插件包 |
| **动态配置** | 插件可声明配置项，宿主自动生成配置表单 |
| **开箱即用** | Windows x64 版本，解压即用，无需额外运行时 |
| **内置插件** | 首次启动自动安装骰子、转盘、时钟、日记、UniEnv 等插件 |

### 技术栈

- **桌面框架**: Electron 34
- **UI 框架**: React 19 + Ant Design 5.23
- **类型系统**: TypeScript 5.7
- **状态管理**: Zustand 5
- **数据库**: SQLite (sql.js WASM)
- **构建工具**: electron-vite 3
- **打包**: electron-builder 25 (NSIS 安装包)

### 内置插件一览

| 插件 | 功能 | 权限 |
|------|------|------|
| DiceRoller | 骰子投掷与随机数生成 | 无 |
| Turntable | 抽奖转盘，自定义奖项权重 | database:read/write |
| Time | 世界时钟、秒表、倒计时 | notification |
| Diary | 日记记录与管理 | database:read/write |
| UniEnv | 开发环境配置管理 | 视具体配置 |

---

## 2. 安装与运行

### 方式一：便携版（解压即用，推荐）

1. 在本地目录中找到（或从 GitHub Releases 下载）OpenBox 便携包
2. 完整解压到任意路径（建议英文路径，如 `D:\OpenBox`）
3. 打开解压后的 `OpenBox` 文件夹
4. **双击 `OpenBox.exe`** 启动应用

> **首次启动提示**：若 Windows SmartScreen 显示"Windows 保护了你的电脑"或"未知发布者"，请点击 **"更多信息" → "仍要运行"**。这是因为应用未购买代码签名证书，属正常安全提示。

### 方式二：安装版（通过安装程序）

1. 双击运行 `openbox-1.0.1-setup.exe`
2. 按照安装向导提示选择安装路径（默认 `C:\Program Files\OpenBox`）
3. 可选是否创建桌面快捷方式
4. 安装完成后，通过桌面快捷方式或开始菜单启动 OpenBox

### 启动后的首次体验

首次启动 OpenBox 时，系统会自动安装所有内置插件（DiceRoller、Turntable、UniEnv、Diary）。安装完成后，首页即可看到已安装的插件卡片列表。后续启动将跳过此过程，不影响已有插件数据。

---

## 3. 界面导览

OpenBox 的主界面分为三个主要区域：

```
┌──────────────────────────────────────────┐
│           标题栏 / 菜单栏                   │
├──────────┬───────────────────────────────┤
│          │                               │
│  左侧    │       右侧内容区域              │
│  导航栏   │                               │
│          │   ┌───┐ ┌───┐ ┌───┐          │
│  🏠 首页  │   │插件│ │插件│ │插件│          │
│  📦 插件  │   └───┘ └───┘ └───┘          │
│     管理  │                               │
│  ⚙ 设置  │                               │
│          │                               │
└──────────┴───────────────────────────────┘
```

### 首页
- 显示所有已安装并**已激活**的插件卡片
- 点击卡片即可进入对应插件的功能界面
- 每个卡片显示插件图标、名称和简短描述

### 插件管理
- **插件列表**：展示所有已安装插件及其状态（已启用/已停用）
- **导入插件**：支持拖拽或点击选择 .zip 插件包
- **插件操作**：对每个插件可进行启用、停用、卸载等操作

### 设置
- 应用全局设置（主题、语言等）
- 各插件的独立配置项

---

## 4. 插件管理

### 4.1 导入/安装插件

**方式一：拖拽导入（推荐）**

1. 点击左侧导航栏进入 **"插件管理"** 页面
2. 将 `.zip` 插件包从文件管理器拖入导入区域
3. 系统自动验证、解压并安装
4. 导入成功后插件**自动激活**，返回首页即可使用

**方式二：点击选择文件**

1. 在插件管理页面点击导入区域的"选择文件"按钮
2. 浏览并选中 `.zip` 插件包
3. 确认导入，等待安装完成

### 4.2 启用 / 停用插件

- 在 **插件管理** 页面，每个插件卡片上提供**启用/停用**切换按钮
- **停用**不会删除插件数据，仅暂停运行
- 停用后插件不再出现在首页

### 4.3 卸载插件

1. 在插件管理页面找到目标插件
2. 点击插件卡片上的**删除**按钮
3. 确认卸载后，插件文件目录和数据库记录将被清除

> **注意**：卸载前建议确认插件内是否有需要保留的数据。

### 4.4 查看插件信息

在插件管理页面点击插件卡片，可查看：
- 插件名称、版本号、作者
- 权限声明列表
- 配置项（若有）

---

## 5. 内置插件使用

### 5.1 DiceRoller（骰子与随机数）

一个轻量级的骰子投掷工具。

| 功能 | 说明 |
|------|------|
| 骰子数量 | 支持 1 ~ 20 个骰子同时投掷 |
| 骰子面数 | 支持 2 ~ 100 面（如 D6、D20 等） |
| 投掷结果 | 显示每个骰子的值和总和 |
| 历史记录 | 自动保留投掷历史 |

**使用步骤**：
1. 首页点击 DiceRoller 卡片
2. 设置骰子数量和面数
3. 点击"投掷"按钮
4. 查看结果和历史记录

**配置项**：默认骰子面数

---

### 5.2 Turntable（转盘抽奖）

基于 Canvas 绘制的抽奖转盘，适合活动抽奖、随机选择等场景。

| 功能 | 说明 |
|------|------|
| 自定义奖项 | 可添加/编辑/删除奖项 |
| 权重设置 | 每个奖项可设置不同的中奖概率 |
| 颜色定制 | 每个奖项可设置独立的颜色 |
| 旋转动画 | 流畅的转盘旋转动画 |
| 结果记录 | 自动保存抽奖结果 |

**使用步骤**：
1. 首页点击 Turntable 卡片
2. 在设置中编辑奖项名称、权重和颜色
3. 点击转盘中心按钮开始旋转
4. 转盘停止后显示中奖结果

**配置项**：主题颜色、旋转时长

---

### 5.3 Time（时钟工具箱）

三合一时间管理工具，包含多个实用功能。

#### 世界时钟
- 支持多个时区同时显示
- 可添加/删除时区
- 显示各时区的当前日期和时间

#### 秒表
- 开始、暂停、继续、重置
- 记录中间计次（Lap）

#### 倒计时
- 自定义倒计时时长
- 倒计时结束时发送系统通知
- 支持预设常用时长

**配置项**：是否显示秒数、是否使用 24 小时制

---

### 5.4 Diary（日记）

轻量级日记记录工具。

| 功能 | 说明 |
|------|------|
| 新建日记 | 按日期创建日记条目 |
| 编辑日记 | 支持富文本编辑 |
| 历史浏览 | 按日期查看历史日记 |
| 搜索 | 按关键词搜索日记内容 |

---

### 5.5 UniEnv（开发环境配置）

开发环境管理工具，帮助开发者统一管理项目环境配置。

> 具体功能请参考插件内部说明文档。

---

## 6. 设置

### 6.1 应用设置

在左侧导航栏点击 **设置** 可进入应用全局设置页面，包括：
- **外观**：主题模式（亮色/暗色/跟随系统）
- **语言**：界面语言（当前支持简体中文）
- **插件目录**：查看当前插件存储路径

### 6.2 插件配置

对于声明了配置项的插件，可在插件管理页面或设置页面进行配置：

- **字符串**：文本输入（如主题颜色代码）
- **数值**：数字输入（如骰子默认面数）
- **布尔值**：开关切换（如是否自动启动）
- **选择**：下拉单选（如时区选择）
- **多选**：多选框（如启用功能模块）

修改配置后即时生效，无需重启插件或应用。

---

## 7. 常见问题 (FAQ)

### Q1: Windows SmartScreen 阻止运行怎么办？

应用未购买代码签名证书，属正常安全提醒。请点击 **"更多信息" → "仍要运行"** 即可。

### Q2: 插件导入后在哪里查看？

导入成功自动激活，返回 **首页** 即可看到已安装的插件卡片。

### Q3: 插件显示"未激活"怎么办？

首次导入的插件会自动激活。如果插件代码抛出异常会导致激活失败。请尝试：
1. 重新导入插件包
2. 检查插件是否为当前版本兼容格式
3. 查看日志排查错误

### Q4: 如何卸载插件？

在 **插件管理** 页面点击插件卡片上的删除按钮，确认后即可卸载。

### Q5: 应用数据和插件存在哪里？

| 数据类型 | 存储位置 |
|----------|----------|
| 插件文件 | `%APPDATA%/OpenBox/plugins/` |
| 数据库（配置、记录） | `%APPDATA%/OpenBox/data/openbox.db` |
| 应用日志 | `%APPDATA%/OpenBox/logs/` |

### Q6: 如何备份和恢复插件？

直接备份 `%APPDATA%/OpenBox/` 目录即可。恢复时将同名目录复制回原位置。

### Q7: 能否在 macOS 或 Linux 上运行？

当前版本仅面向 **Windows x64** 构建。由于依赖 Electron 等跨平台技术，理论上可移植，但未经过充分测试。

### Q8: 应用会收集我的数据吗？

OpenBox 是纯本地应用，所有数据存储在本地，**不会**收集或上传任何用户数据。详情请查看 LICENSE 文件。

### Q9: 如何更新 OpenBox？

请关注 GitHub Releases 页面下载最新版本。更新时建议先备份 `%APPDATA%/OpenBox/` 目录，再安装新版本。

### Q10: 如何获取帮助或报告问题？

请访问 GitHub 仓库提交 Issue：<https://github.com/Bistu-OSSDT-2026/OpenBox/issues>

---

## 8. 附录

### 8.1 从源码构建

如需自行从源码构建：

#### 环境要求
- **Node.js** >= 20
- **npm**（随 Node.js 安装）

#### 构建步骤

```bash
# 1. 克隆仓库
git clone https://github.com/Bistu-OSSDT-2026/OpenBox.git

# 2. 进入源码目录
cd OpenBox/Source/OpenBox

# 3. 安装依赖
npm install

# 4. 开发模式运行（带热重载）
npm run dev

# 5. 类型检查（可选）
npm run typecheck

# 6. 生产构建
npm run build

# 7. 打包为可分发安装包
npm run package
```

构建产物位于 `release/` 目录。

### 8.2 目录结构说明

```
OpenBox-Portable/         ← Windows x64 便携版
├── OpenBox.exe           ← 主程序入口
└── resources/
    └── app.asar          ← 打包后的应用代码
```

### 8.3 相关链接

- **GitHub 仓库**: <https://github.com/Bistu-OSSDT-2026/OpenBox>
- **Releases**: <https://github.com/Bistu-OSSDT-2026/OpenBox/releases>
- **许可证**: MIT License

---

### 8.4 许可信息

```
MIT License

Copyright (c) 2025-2026 openbox

本软件为开源教学项目，遵循 MIT 许可证发布。
您可以自由使用、修改和分发，详见 LICENSE 文件。
```

---

*文档生成日期：2026 年 7 月 9 日*
