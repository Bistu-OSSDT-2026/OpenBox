# UniEnv — 开发环境配置工具插件 PRD

> **版本:** v0.3.2
> **状态:** 草稿
> **日期:** 2026-07-07
> **所属平台:** OpenBox 开源工具箱

---

## 1. 文档概述

### 1.1 目的

本文档定义 UniEnv 插件（以下简称"本插件"）的产品需求。UniEnv 是 OpenBox 生态中的一款插件，面向编程初学者，实现在 Windows 平台上一键检测、安装、切换与卸载常见开发工具。

### 1.2 范围

本文档覆盖 v0.3.2 的全部功能，包括工具管理(检测/安装/多版本切换/卸载)、组合包一键安装、下载镜像配置、进度反馈。不涉及远程插件市场、自动更新、跨平台支持。

### 1.3 术语表

| 术语 | 说明 |
|------|------|
| 工具 (Tool) | 一个受管理的开发工具实例，如 Python、Node.js |
| 版本 (Version) | 工具的特定版本号 |
| 组合包 (Combo Pack) | 预定义的多个工具+版本的集合，支持一键安装 |
| 镜像 (Mirror) | 软件分发的镜像站点，用于加速下载 |
| 目录链接 (Junction) | Windows NTFS 目录符号链接，用于多版本切换 |
| 安装根目录 (Install Root) | 所有工具安装在此目录下，默认 `C:\UniEnv` |

---

## 2. 产品定位

### 2.1 背景

编程初学者在配置开发环境时面临诸多痛点：
- 不知道去哪里下载工具
- 下载速度慢（海外源）
- 安装过程复杂，配置路径繁琐
- 多版本共存/切换困难
- 卸载不干净留下残余

UniEnv 旨在将所有开发环境的配置流程简化为 **"选择 → 点击 → 等待完成"** 三步，让初学者专注编程本身。

### 2.2 目标用户

| 用户类型 | 特征 | 核心诉求 |
|----------|------|----------|
| 编程初学者 | 零基础或刚入门，首次配置环境 | 一键安装、无需手动配置、清晰的引导 |
| 日常开发者 | 需要维护多语言/多版本环境 | 快速切换版本、批量安装、无残留卸载 |
| 教学场景 | 教师/培训机构的统一环境配置 | 组合包批量部署、镜像加速 |

### 2.3 核心价值

- **零门槛** — 无需浏览官网、无需配置 PATH，点击即装
- **多版本管理** — 基于 Junction 的多版本共存与快速切换
- **镜像加速** — 内置华为云、阿里云、清华 TUNA 等多个国内镜像
- **组合安装** — 一键批量安装相关系列工具（如 Python 全栈、Java 开发）

---

## 3. 功能需求

### 3.1 工具管理

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| TM-01 | 工具列表 | P0 | 展示所有受支持的工具（Python、Node.js、Git、Go、Java），含图标、名称、描述、安装状态指示灯 |
| TM-02 | 检测安装状态 | P0 | 自动检测工具是否已安装及当前版本，支持系统 PATH 检测和自管理目录检测 |
| TM-03 | 版本列表 | P0 | 展示工具可供安装的版本列表 |
| TM-04 | 安装工具 | P0 | 选择版本后下载并静默安装到指定目录，安装完成后创建 version→runtime 的目录链接 |
| TM-05 | 切换版本 | P0 | 在多版本之间切换 active 版本，通过重新指向 Junction 实现 |
| TM-06 | 卸载工具 | P0 | 移除 Junction 链接（保留版本目录数据），提供二次确认 |
| TM-07 | 安装进度反馈 | P0 | 实时展示下载/解压/配置阶段的进度百分比和状态文字 |
| TM-08 | 错误处理 | P1 | 安装失败时反馈错误原因，不影响其他工具运行 |

### 3.2 组合包

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| CP-01 | 内置组合包 | P0 | 预定义 5 种组合包：Python 全栈、Java 开发、Go 开发、前端开发、全栈通用 |
| CP-02 | 组合包一键安装 | P0 | 按顺序批量安装组合包内所有工具，逐项反馈结果 |
| CP-03 | 自定义组合包 | P1 | 用户通过 JSON 配置自定义组合包的内容 |
| CP-04 | 组合包快速入口 | P1 | 底部栏展示前 5 个组合包的快捷安装按钮 |

### 3.3 配置

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| CF-01 | 安装根目录 | P0 | 配置所有工具的安装目标路径，默认 `C:\UniEnv` |
| CF-02 | 下载镜像选择 | P0 | 支持 direct(官方)、huawei(华为云)、aliyun(阿里云)、tuna(清华) 四种镜像 |
| CF-03 | 自定义组合包配置 | P1 | JSON 格式的扩展组合包配置 |

### 3.4 视觉与交互

| ID | 需求 | 优先级 | 说明 |
|----|------|--------|------|
| VF-01 | 三栏布局 | P0 | 左侧工具/组合包菜单、中间状态看板、右侧操作面板 |
| VF-02 | 安装状态指示 | P0 | 菜单项右侧圆点（绿色=已安装、灰色=未安装） |
| VF-03 | 组合包详情 | P0 | 展示组合包内各项工具的安装状态 |
| VF-04 | 进度条 | P0 | 中间面板内展示实时进度条和状态文字 |
| VF-05 | Toast 通知 | P0 | 右上角弹出成功/错误/警告/信息提示，3.5 秒自动消失 |
| VF-06 | 确认弹窗 | P0 | 卸载和组合包安装前弹出确认对话框 |

---

## 4. 非功能需求

| ID | 需求 | 指标 |
|----|------|------|
| NF-01 | 插件激活时间 | 主进程入口加载 < 200ms |
| NF-02 | 检测耗时 | 单工具检测 < 10s（含系统调用超时） |
| NF-03 | 下载超时 | 单次下载请求超时 120s，自动重试 2 次 |
| NF-04 | 安装超时 | 静默安装进程超时 600s |
| NF-05 | 磁盘占用 | 插件本体 < 5MB，运行时缓存 < 50MB |
| NF-06 | 兼容性 | Windows 10/11 x64，依赖 PowerShell 和 mklink |
| NF-07 | 安全性 | 安装路径防注入检查、子进程超时控制 |

---

## 5. 系统架构

### 5.1 模块架构

```
┌────────────────────────────────────────────────────┐
│                  渲染进程 (Renderer)                  │
│  React 19 (内联样式, 无第三方UI库)                   │
│  ┌────────────────────────────────────────────┐    │
│  │  UniEnvUI                                  │    │
│  │  三栏布局: 菜单 / 看板 / 操作区               │    │
│  │  状态管理: useState + useCallback           │    │
│  └──────────────┬─────────────────────────────┘    │
│                 │ sendToBackend / onBackendMessage   │
└─────────────────┼──────────────────────────────────┘
                  │ IPC (OpenBox 插件协议)
┌─────────────────┼──────────────────────────────────┐
│              主进程 (Main)                           │
│  ┌────────────────────────────────────────────┐    │
│  │ Plugin (activate / deactivate / onMessage) │    │
│  │                                            │    │
│  │  Tools Map                Progress Map     │    │
│  │  ├─ pythonTool            install tasks    │    │
│  │  ├─ nodeTool                               │    │
│  │  ├─ gitTool                                │    │
│  │  ├─ goTool                                 │    │
│  │  └─ javaTool                               │    │
│  └───────────┬────────────────────────────────┘    │
│              │                                      │
│  ┌───────────┴────────────────────────────────┐    │
│  │ tools/base.ts (公共基础设施)                  │    │
│  │  asyncExec / fetchWithTimeout               │    │
│  │  downloadWithProgress / downloadWithFallback│    │
│  │  extractZip / createJunction / removeJunction│    │
│  └─────────────────────────────────────────────┘    │
│                                                     │
│  权限声明: shell:exec / network:fetch               │
│            notification / dialog                     │
└────────────────────────────────────────────────────┘
```

### 5.2 核心模块职责

| 模块 | 职责 |
|------|------|
| `main.ts` | 插件入口，注册 5 个 ToolDef、处理所有消息路由、管理后台安装任务进度 |
| `renderer.tsx` | 完整的三栏式 UI，包括工具列表、状态看板、操作面板、组合包、Toast |
| `tools/base.ts` | 定义 ToolDef 接口、下载/重试/解压/目录操作等公共工具函数 |
| `tools/python.ts` | Python 检测/版本列表/安装/卸载/切换实现 |
| `tools/node.ts` | Node.js 检测/版本列表/安装/卸载/切换实现 |
| `tools/git.ts` | Git 检测/版本列表/安装/卸载/切换实现 |
| `tools/go.ts` | Go 检测/版本列表/安装/卸载/切换实现 |
| `tools/java.ts` | Java JDK 检测/版本列表/安装/卸载/切换实现 |
| `combo.ts` | 内置组合包定义 + `resolveTool` 工具函数 |
| `openbox-api.d.ts` | OpenBox 插件 API 类型声明 |

### 5.3 数据流

```
检测流程:
  Renderer → send({type:'detect', tool:'python'})
  → Main: tool.detect(installRoot)
  → 系统 PATH / 自定义目录 → 返回 {installed, version, path}

安装流程:
  Renderer → send({type:'install', tool:'python', version:'3.11.9'})
  → Main: startBackgroundInstall()
    → progressMap 标记开始
    → 异步: 下载 → 解压/静默安装 → 创建 Junction
    → progressMap 标记完成
  → Renderer 轮询 getProgress → 更新进度条

版本切换:
  Renderer → send({type:'switchVersion', tool:'python', version:'3.12.5'})
  → Main: 重新创建 Junction(versionDir→current)
  → 完成

组合包安装:
  Renderer → send({type:'installCombo', comboId:'python-fullstack'})
  → Main: 依次安装 combo.items 中的每一项
  → 返回逐项成功/失败结果
```

### 5.4 下载镜像策略

每个工具定义多个下载 URL，按优先级排列：

```
当前镜像配置 → 对应镜像 URL → 其他镜像 URL → 官方原版 URL (兜底)
```

`downloadWithFallback` 依次尝试每个 URL，前一个失败后自动切换下一个，全部失败则抛出异常。

---

## 6. 用户交互流程

### 6.1 首次使用

```
安装插件 → 打开 UniEnv → 左侧看到工具列表(灰色圆点=未安装)
→ 点击任一工具 → 右侧面板选择版本 → 点击「安装」
→ 中间看板显示进度条和状态文字
→ 安装完成 → 圆点变绿 → 可继续安装其他工具
```

### 6.2 日常使用

```
写 Python 新项目需要 3.12:
→ 打开 UniEnv → 选择 Python → 版本下拉选 3.12.5
→ 点击「切换版本」→ 完成

批量安装:
→ 点击底部「Python 全栈」快捷按钮
→ 确认 → Python 3.11 + Node 18 + Git 依次安装
```

### 6.3 卸载

```
选择已安装工具 → 点击「卸载」→ 确认对话框
→ 移除 Junction 链接 → 完成
(版本数据保留在磁盘，如需清理需手动删除目录)
```

---

## 7. 约束与假设

### 7.1 技术约束

- 仅支持 Windows x64 平台（依赖 `mklink /J` 和 `Expand-Archive`）
- 使用 Node.js `child_process.exec` 调用系统命令
- 渲染进程 UI 为内联样式，无第三方 UI 库依赖
- 插件通过 OpenBox 的 `onMessage` IPC 模型与渲染进程通信

### 7.2 设计假设

- 目标用户主要在国内，镜像下载为刚需
- 工具版本列表为硬编码（非动态爬取），需随插件版本更新
- 用户网络环境可能不稳定，下载含自动重试和多源兜底

### 7.3 边界条件

| 场景 | 处理方式 |
|------|----------|
| 工具已在系统 PATH 中 | 检测到后标记为已安装，不再管理 |
| 下载全部失败 | 抛出含所有尝试源信息的错误 |
| 静默安装被用户中断 | 子进程超时 600s 后放弃 |
| 磁盘空间不足 | 由底层系统调用报错，向上透传 |
| 安装过程中关闭插件 | 后台安装继续完成，下次打开可通过检测获取状态 |

---

## 8. 未来迭代方向

| 方向 | 说明 | 优先级 |
|------|------|--------|
| 支持更多工具 | Rust、Docker、Flutter 等 | P1 |
| 动态版本获取 | 从官方 API 自动拉取可用版本列表 | P1 |
| 工具更新提示 | 检测到新版本时通知用户 | P2 |
| PATH 自动管理 | 安装后自动将 current 加入系统 PATH | P2 |
| 局域网 P2P 分发 | 局域网内共享已下载的安装包 | P3 |
| 安装包缓存管理 | 清理历史版本的下载缓存 | P3 |
| 无人值守批量部署 | 通过 CLI 参数自动安装组合包 | P2 |
| 语言本地化 | 中文/英文界面切换 | P2 |

---

## 9. 附录

### 9.1 参考文档

- [OpenBox PRD-OpenBox-Toolbox.md](https://github.com/Bistu-OSSDT-2026/OpenBox/blob/main/PRD-OpenBox-Toolbox.md)
- [OpenBox 插件开发文档](https://github.com/Bistu-OSSDT-2026/OpenBox)

### 9.2 修订历史

| 版本 | 日期 | 说明 | 作者 |
|------|------|------|------|
| v0.1 | 2026-07-07 | 初稿 | Evolve28 |
